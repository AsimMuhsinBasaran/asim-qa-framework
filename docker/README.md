# Docker

This directory is reserved for local infrastructure support files.

It can be used later for:
- containerized dependencies
- local test environments
- compose files
- service bootstrap scripts

The directory is present now so the monorepo structure is explicit before infrastructure work begins.
